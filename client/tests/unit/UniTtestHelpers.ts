/**
 * Contains stub and mock helpers for unit tests
 *
 * @since app-login--JP
 */

export const stubSessionModule = {
  getAppSessionPayload: jest.fn(),
};

export const mockSessionModule = {
  getAppSessionPayload: jest.fn(),
};