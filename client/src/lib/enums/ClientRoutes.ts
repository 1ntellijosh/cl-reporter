/**
 * Client routes strings enum.
 *
 * @since app-login--JP
 */

export enum CL_ROUTES {
  REPORTS_DASHBOARD = '/dashboard',
  ERROR = '/error',
  OAUTH_START = '/start',
  OAUTH_CALLBACK = '/api/auth/clover-callback',
  COMPLETE_CLOVER = '/api/auth/complete-clover',
}
